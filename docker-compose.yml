# Suite SaaS Mini: 8 productos sobre una misma imagen (core + build).
# Cada servicio corre un workspace y persiste su SQLite en SU PROPIO volumen
# (saasmini_data_<producto>): aislamiento — un contenedor comprometido no
# puede leer ni escribir las bases de otro cliente (sin volumen compartido).
# Los JWT vienen del archivo .env de compose (generado en el servidor, NO commiteado).

x-app: &app
  image: saas-mini:latest
  build: .
  restart: unless-stopped
  volumes:
    # Puerta de acceso: edita ops/clients.json en el servidor para activar/pausar clientes.
    # Al ser en vivo (bind mount), el cambio aplica sin rebuild ni reinicio de servicios.
    - ./ops/clients.json:/app/clients.json:ro
  environment: &base-env
    NODE_ENV: production

services:
  peluqueria:
    <<: *app
    container_name: saasmini-peluqueria
    volumes:
      - saasmini_data_peluqueria:/app/data/peluqueria
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3100:3000"
    environment:
      <<: *base-env
      PRODUCT: peluqueria
      PORT: "3000"
      DB_PATH: /app/data/peluqueria/app.db
      JWT_SECRET: ${PELU_JWT:?falta PELU_JWT en .env}
      APP_NAME: Agenda de Citas
      APP_URL: https://agenda.amgdeveloper.cl

  deportes:
    <<: *app
    container_name: saasmini-deportes
    volumes:
      - saasmini_data_deportes:/app/data/deportes
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3101:3000"
    environment:
      <<: *base-env
      PRODUCT: deportes
      PORT: "3000"
      DB_PATH: /app/data/deportes/app.db
      JWT_SECRET: ${DEPO_JWT:?falta DEPO_JWT en .env}
      APP_NAME: Reservas de Canchas
      APP_URL: https://canchas.amgdeveloper.cl

  talleres:
    <<: *app
    container_name: saasmini-talleres
    volumes:
      - saasmini_data_talleres:/app/data/talleres
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3102:3000"
    environment:
      <<: *base-env
      PRODUCT: talleres
      PORT: "3000"
      DB_PATH: /app/data/talleres/app.db
      JWT_SECRET: ${TALLE_JWT:?falta TALLE_JWT en .env}
      APP_NAME: Órdenes de Trabajo
      APP_URL: https://ordenes.amgdeveloper.cl

  inventario:
    <<: *app
    container_name: saasmini-inventario
    volumes:
      - saasmini_data_inventario:/app/data/inventario
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3103:3000"
    environment:
      <<: *base-env
      PRODUCT: inventario
      PORT: "3000"
      DB_PATH: /app/data/inventario/app.db
      JWT_SECRET: ${INV_JWT:?falta INV_JWT en .env}
      APP_NAME: Almacenaje
      APP_URL: https://stock.amgdeveloper.cl

  cotizaciones:
    <<: *app
    container_name: saasmini-cotizaciones
    volumes:
      - saasmini_data_cotizaciones:/app/data/cotizaciones
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3104:3000"
    environment:
      <<: *base-env
      PRODUCT: cotizaciones
      PORT: "3000"
      DB_PATH: /app/data/cotizaciones/app.db
      JWT_SECRET: ${COTI_JWT:?falta COTI_JWT en .env}
      APP_NAME: Cotizaciones y Presupuestos
      APP_URL: https://presupuestos.amgdeveloper.cl

  documentos:
    <<: *app
    container_name: saasmini-documentos
    volumes:
      - saasmini_data_documentos:/app/data/documentos
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3105:3000"
    environment:
      <<: *base-env
      PRODUCT: documentos
      PORT: "3000"
      DB_PATH: /app/data/documentos/app.db
      JWT_SECRET: ${DOCU_JWT:?falta DOCU_JWT en .env}
      APP_NAME: Documentos y Comprobantes
      APP_URL: https://docs.amgdeveloper.cl

  recordatorios:
    <<: *app
    container_name: saasmini-recordatorios
    volumes:
      - saasmini_data_recordatorios:/app/data/recordatorios
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3106:3000"
    environment:
      <<: *base-env
      PRODUCT: recordatorios
      PORT: "3000"
      DB_PATH: /app/data/recordatorios/app.db
      JWT_SECRET: ${ALER_JWT:?falta ALER_JWT en .env}
      APP_NAME: Recordatorios
      APP_URL: https://recordatorios.amgdeveloper.cl

  crm:
    <<: *app
    container_name: saasmini-crm
    volumes:
      - saasmini_data_crm:/app/data/crm
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3107:3000"
    environment:
      <<: *base-env
      PRODUCT: crm
      PORT: "3000"
      DB_PATH: /app/data/crm/app.db
      JWT_SECRET: ${CRM_JWT:?falta CRM_JWT en .env}
      APP_NAME: Gestión de Clientes
      APP_URL: https://clientes.amgdeveloper.cl

  landing:
    <<: *app
    container_name: saasmini-landing
    volumes:
      - saasmini_data_landing:/app/data/landing
      - ./ops/clients.json:/app/clients.json:ro
    ports:
      - "127.0.0.1:3108:3000"
    environment:
      <<: *base-env
      PRODUCT: landing
      PORT: "3000"
      DB_PATH: /app/data/landing/app.db
      JWT_SECRET: ${LAND_JWT:?falta LAND_JWT en .env}
      APP_NAME: AMG
      APP_URL: https://amgdeveloper.cl

volumes:
  saasmini_data_peluqueria:
  saasmini_data_deportes:
  saasmini_data_talleres:
  saasmini_data_inventario:
  saasmini_data_cotizaciones:
  saasmini_data_documentos:
  saasmini_data_recordatorios:
  saasmini_data_crm:
  saasmini_data_landing:
