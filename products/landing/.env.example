NODE_ENV=development
PORT=3008

# La landing no usa base de datos: el landing elige un mapa estático a cada subdominio.

# IMPORTANTE: cambia esto por una clave larga y aleatoria en producción
JWT_SECRET=cambia-esta-clave-en-produccion-por-favor

APP_NAME=AMG
# En producción la URL pública es https://desarrollo.amgdeveloper.cl (docker-compose)
APP_URL=http://localhost:3008